node_modules/
cypress/reports/
cypress/screenshots/
cypress/videos/
cypress/downloads/

# Playwright generated output
playwright-report/
test-results/

# Auth sessions — never commit these
.auth/

# Claude Code internals
.claude/worktrees/

# Credentials — never commit these
email.config.json
cypress.env.json
.env*
