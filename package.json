{
  "name": "auriga-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report",
    "test:dev": "npx playwright test --project=dev",
    "test:uat": "npx playwright test --project=uat",
    "test:parallel": "npx playwright test --project=uat --fully-parallel --workers=10",
    "test:fast": "npx playwright test --project=uat --fully-parallel --workers=100%",
    "test:shard": "npx playwright test --project=uat --shard=1/4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "dotenv": "^17.4.2",
    "rimraf": "^5.0.5",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "nodemailer": "^8.0.6",
    "papaparse": "^5.5.3"
  }
}
